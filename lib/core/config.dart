// Temporary development mode configuration
// Toggle this flag to enable/disable the simplified development
// authentication flows. Set to `false` before releasing to production.
const bool kDevelopmentAuthMode = true;
